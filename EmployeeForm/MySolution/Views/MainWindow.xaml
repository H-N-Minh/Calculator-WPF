<Window x:Class="MySolution.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:MySolution"
        xmlns:vm="clr-namespace:MySolution.ViewModels"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        ThemeMode="Light">
    <Window.DataContext>
        <vm:MainViewModel/>
    </Window.DataContext>
    
    <Grid ColumnDefinitions="1*, 1*">
        <StackPanel Grid.Column="0"
                    DataContext="{Binding CurrentEmployee}"
                    Margin="10">
            <TextBlock Text="Name:"/>
            <TextBox Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Email:"/>
            <TextBox Text="{Binding Email, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Age:"/>
            <TextBox Text="{Binding Age, UpdateSourceTrigger=PropertyChanged}"/>
            <TextBlock Text="Department:"/>
            <ComboBox ItemsSource="{Binding AllDepartments}"
                      SelectedItem="{Binding Department}"/>

            <TextBlock Text="Summary:"/>
            <TextBlock Text="{Binding Summary}"/>
            <Button Content="Submit"
                    Command="{Binding SubmitCommand}"/>

        </StackPanel>

        <ListBox Grid.Column="1"
                 ItemsSource="{Binding Employees}">
            <ListBox.ItemsPanel>
                <ItemsPanelTemplate>
                    <StackPanel/>
                </ItemsPanelTemplate>
            </ListBox.ItemsPanel>

            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="{Binding Name}" Margin="5"/>
                        <TextBlock Text="{Binding Email}"  Margin="5"/>
                        <TextBlock Text="{Binding Age}" Margin="5"/>
                        <TextBlock Text="{Binding Department}" Margin="5"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
    </Grid>
</Window>
