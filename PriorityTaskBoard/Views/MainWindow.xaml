<Window x:Class="PriorityTaskBoard.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:PriorityTaskBoard.ViewModels"
        xmlns:controls="clr-namespace:PriorityTaskBoard.Controls"
        xmlns:behaviors="clr-namespace:PriorityTaskBoard.Behaviours"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="600"
        Background="{DynamicResource WindowBackground}">
    
    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Theme Controls -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,15">
            <Button Content="Light Theme" Click="LightTheme_Click" Margin="0,0,5,0" Padding="10,5"/>
            <Button Content="Dark Theme" Click="DarkTheme_Click" Padding="10,5"/>
        </StackPanel>

        <!-- Input Area (Attached Property in use) -->
        <Grid Grid.Row="1" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0"
                       Text="Insert task here..."
                       Background="Transparent"
                       Padding="5"
                       IsHitTestVisible="False">
                <TextBlock.Style>
                    <Style TargetType="TextBlock">
                        <Setter Property="Background" Value="White" />
                        <Setter Property="Visibility" Value="Visible"/>
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsUserTyping}" Value="True">
                                <Setter Property="Visibility" Value="Hidden"/>
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </TextBlock.Style>
            </TextBlock>

            <TextBox Grid.Column="0" 
                     Text="{Binding NewTaskTitle, UpdateSourceTrigger=PropertyChanged}" 
                     behaviors:TextBoxBehavior.SelectAllOnFocus="True"
                     Padding="5" VerticalContentAlignment="Center"
                     Panel.ZIndex="-1"/>

            <ComboBox Grid.Column="1" 
                      ItemsSource="{Binding AvailablePriorities}" 
                      SelectedItem="{Binding SelectedPriority}"
                      Margin="10,0" Width="100" Padding="5" VerticalContentAlignment="Center"/>

            <Button Grid.Column="2" 
                    Content="Add Task" 
                    Command="{Binding AddTaskCommand}" 
                    Padding="15,5"/>
        </Grid>

        <!-- Task List (ListBox & DataTemplate) -->
        <ListView Grid.Row="2" 
                 ItemsSource="{Binding Tasks}" 
                 Background="{DynamicResource PanelBackground}" 
                 BorderThickness="0">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <!-- Custom UserControl (Dependency Property in use) -->
                    <controls:SingleEntry 
                        IsChecked="{Binding IsCompleted}"
                        Text="{Binding Title}"
                        PriorityLevel="{Binding Priority}" 
                        />
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Window>
