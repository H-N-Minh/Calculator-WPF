<UserControl x:Class="SDK.Views.MainListView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:SDK.ViewModels"
             xmlns:behav="clr-namespace:SDK.Behaviors"
             d:DataContext="{d:DesignInstance Type=vm:MainListViewModel}">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <!-- Action buttons -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,0,0,10">
            <Button Content="Add" Command="{Binding AddBookCommand}" Width="60" Margin="5,0" />
            <Button Content="Remove" Command="{Binding RemoveBooksCommand}" Width="60" Margin="5,0">
                <Button.Style>
                    <Style TargetType="Button">
                        <Setter Property="Foreground" Value="Black" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding SelectedBooks.Count}" Value="0">
                                <Setter Property="IsEnabled" Value="False" />
                                <Setter Property="Foreground" Value="Gray" />
                            </DataTrigger>
                            <DataTrigger Binding="{Binding SelectedBooks.Count}" Value="1">
                                <Setter Property="Foreground" Value="Red" />
                            </DataTrigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="Gray" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Button.Style>
            </Button>
        </StackPanel>

        <ListView Grid.Row="1" ItemsSource="{Binding Books}"
                  SelectionMode="Extended"
                  behav:ListViewSelectedItems.SelectedItems="{Binding SelectedBooks}">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                        </Grid.ColumnDefinitions>
                        <TextBlock VerticalAlignment="Center">
                            <Run Text="{Binding Name}" />
                            <Run Text=" - " />
                            <Run Text="{Binding Author, TargetNullValue='(unknown)'}" />
                        </TextBlock>
                        <Button Grid.Column="1" Content="View" Width="50" Margin="5,0"
                                Command="{Binding DataContext.ViewBookCommand, RelativeSource={RelativeSource AncestorType=ListView}}"
                                CommandParameter="{Binding}" />
                    </Grid>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>