<Window x:Class="YT.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:YT"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        d:DataContext="{d:DesignInstance Type=local:MainWindow, IsDesignTimeCreatable=True}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <!--Search box-->
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
               
            <!--Text box-->
            <Grid Grid.Column="0">
                <TextBlock VerticalAlignment="Center" Margin="15,20,10,20" Text="Insert a book name" Panel.ZIndex="-1" Name="UITextBlock"/>
                <TextBox VerticalAlignment="Center"  Margin="10,20,10,20" Background="Transparent"/>

                <Button Content="X" Width="30" HorizontalAlignment="Right" VerticalAlignment="Center" 
                        Background="Transparent" Foreground="Gray" BorderThickness="0" Margin="0,0,10,0"/>
            </Grid>
            
            <!--Search button-->
            <Grid Grid.Column="1" Margin="10">
                <Button Content="Search" Width="50" HorizontalAlignment="Right" Background="LightBlue"/>
            </Grid>
        </Grid>

    </Grid>
</Window>
