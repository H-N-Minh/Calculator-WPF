<Window x:Class="Library.Views.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:vm="clr-namespace:Library.ViewModels"
        xmlns:view="clr-namespace:Library.Views"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800"
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel, IsDesignTimeCreatable=True}">

    <Window.Resources>
        <DataTemplate DataType="{x:Type vm:BooksListVM}">
            <view:BookListViews></view:BookListViews>
        </DataTemplate>
    </Window.Resources>
    
    <ContentPresenter Content="{Binding Screen}"/>
    
</Window>
